import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel variant='h1' children='Give Back' />
        </div>
    );
}

export default Page;