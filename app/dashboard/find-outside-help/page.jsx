import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel variant='h1' children='Find Outside Help' />
        </div>
    );
}

export default Page;