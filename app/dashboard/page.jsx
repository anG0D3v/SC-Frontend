import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel children='Dashboard' variant='h1' />
        </div>
    );
}

export default Page;