import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel variant='h1' children='Roles and Permissions' />
        </div>
    );
}

export default Page;