import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel variant='h1' children='Profile' />
        </div>
    );
}

export default Page;

