import { CustomLabel } from '@/components';

function Page(props) {
    return (
        <div>
            <CustomLabel variant='h1' children='Community Configuration' />
        </div>
    );
}

export default Page;